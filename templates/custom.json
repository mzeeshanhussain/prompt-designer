{
    "name": "Custom",
    "description": "Generic webhook/API sender. Add optional headers or body.",
    "url": "{{CustomUrl}}",
    "method": "{{GET or POST}}",
    "headers": {
      "Content-Type": "application/json",
      "Authorization": "Bearer {{Token}}"
    },
    "body": {
      "data": "{{UserData}}"
    }
  }
  